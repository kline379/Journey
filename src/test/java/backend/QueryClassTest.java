/*
package backend;

import org.junit.Test;
import static org.junit.Assert.*;

import java.util.List;
import java.util.ArrayList;


public class QueryClassTest {

	final String className = "class,name";
	final double score = 5.7;

	QueryClass testQueryClass= new QueryClass(className, score);

    @Test
    public void testClasses() throws Exception {
		List<String> testList = new ArrayList<String>();
		testList.add(0, "class");
		testList.add(1, "name");

		assertEquals(testQueryClass.Classes(), testList);
	}

	@Test
    public void testRawClass() throws Exception {
		String testRawClass = className;
		assertEquals(testQueryClass.RawClass(), testRawClass);
	}

	@Test
    public void testScore() throws Exception {
		double testScore = score;
		assertEquals(testQueryClass.Score(), testScore);
	}
}
*/
